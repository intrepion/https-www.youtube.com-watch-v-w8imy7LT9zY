@page "/admin/UrlNamePlaceholder-list"
@inject IGameAdminService GameAdminService
@attribute [StreamRendering]

<section>
    <h1>Game List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    @* ListHeaderCodePlaceholder *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (TableLowercaseNamePlaceholder == null)
                {
                    <tr>
                        @* LoadingCountCodePlaceholder *@
                    </tr>
                }
                else if (TableLowercaseNamePlaceholder.Count == 0)
                {
                    <tr>
                        @* ZeroCountCodePlaceholder *@
                    </tr>
                }
                else
                {
                    @foreach (var game in TableLowercaseNamePlaceholder)
                    {
                        <tr>
                            <td>@game.Id</td>
                            @* ListItemCodePlaceholder *@
                            <td>
                                <NavButton Href="@("/admin/UrlNamePlaceholder-edit/" + game.Id)" Name="Edit" />
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <NavButton Href="/admin/UrlNamePlaceholder-edit" Name="Create" />
    </article>
</section>

@code {
    List<Game>? TableLowercaseNamePlaceholder = null;

    protected override async Task OnInitializedAsync()
    {
        TableLowercaseNamePlaceholder = await GameAdminService.GetAllAsync();
    }
}
