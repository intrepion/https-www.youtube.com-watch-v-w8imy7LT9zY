@page "/admin/game-list"
@rendermode InteractiveServer
@inject IGameAdminService GameAdminService
@attribute [StreamRendering]

<section>
    <h1>Game List</h1>
    <article>
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    @* ListHeaderCodePlaceholder *@
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (games == null)
                {
                    <tr>
                        @* LoadingCountCodePlaceholder *@
                    </tr>
                }
                else if (games.Count == 0)
                {
                    <tr>
                        @* ZeroCountCodePlaceholder *@
                    </tr>
                }
                else
                {
                    @foreach (var game in games)
                    {
                        <tr>
                            <td>@game.Id</td>
                            @* ListItemCodePlaceholder *@
                            <td>
                                <NavLink Href="@("/admin/game-edit/" + game.Id)">Edit</NavLink>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <NavLink Href="/admin/game-edit">Create</NavLink>
    </article>
</section>

@code {
    List<GameAdminDataTransferObject>? games = null;

    protected override async Task OnInitializedAsync()
    {
        games = await GameAdminService.GetAllAsync();
    }
}
